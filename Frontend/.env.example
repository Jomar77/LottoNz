# Base URL of the local FastAPI prediction server (see ../api/).
# Run it with: python -m uvicorn api.main:app --reload
# Only used by the "Strategies" (live) generation engine; the default
# Bell Curve / Blend engines and the static predictions.json view work
# without it.
VITE_API_BASE_URL=http://localhost:8000
